# 비밀키를 email로 전송함

type Mutation {
  confirmSecondaryPayment(
    shopId: String!
    ConfirmSecondaryPayment: Boolean
    addressId: String
    ShopEstimatesId: String!
    secondPaypal: Int
    ShippingType: String
  ): Boolean!
}
